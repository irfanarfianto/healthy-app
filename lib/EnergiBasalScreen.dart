import 'package:flutter/material.dart';

class EnergiBasalScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Energi Basal'),
      ),
      body: Center(
        child: Text('Ini adalah detail untuk Energi Basal'),
      ),
    );
  }
}
